services:
  # PostgreSQL
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: vlab-postgres
  #   environment:
  #     POSTGRES_USER: vlabuser
  #     POSTGRES_PASSWORD: vlabpass123
  #     POSTGRES_DB: vlabdb
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U vlabuser -d vlabdb"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - vlab-network

  # # Redis Cache
  # redis:
  #   image: redis:7-alpine
  #   container_name: vlab-redis
  #   ports:
  #     - "6379:6379"
  #   command: redis-server --appendonly yes
  #   volumes:
  #     - redis_data:/data
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - vlab-network

  # FastAPI
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vlab-backend
    environment:
      DATABASE_URL: postgresql://vlabuser:vlabpass123@postgres:5432/vlabdb
      REDIS_URL: redis://redis:6379/0
      PYTHONUNBUFFERED: 1
    ports:
      - "8001:8001"
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    #   redis:
    #     condition: service_healthy
    volumes:
      - .:/app
    # networks:
    #   - vlab-network

# volumes:
#   postgres_data:
#   redis_data:

# networks:
#   vlab-network:
#     driver: bridge
